<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NFC for the focus mechanic -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <!--
      Bluetooth: the API split changed in Android 12 (API 31).
      - On Android up to 11, the install-time BLUETOOTH / BLUETOOTH_ADMIN are used.
      - On Android 12 and newer, the runtime BLUETOOTH_CONNECT is used. We do NOT
        request BLUETOOTH_SCAN because we restrict ourselves to already-paired
        devices, no device discovery is performed by the app.
      The maxSdkVersion attributes on the legacy permissions tell newer Android
      to ignore them, which keeps app permission listings clean.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application>
        <activity
            android:name="com.example.hybridfight.NfcUnityActivity"
            android:label="@string/app_name"
            android:theme="@style/BaseUnityGameActivityTheme"
            android:screenOrientation="portrait"
            android:launchMode="singleTask"
            android:configChanges="fontScale|keyboard|keyboardHidden|locale|mnc|mcc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|touchscreen"
            android:exported="true"
            android:hardwareAccelerated="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data android:name="android.app.lib_name" android:value="game" />
        </activity>
    </application>
</manifest>
